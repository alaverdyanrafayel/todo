export enum FilterBy {
  all = 'all',
  completed = 'completed',
  uncompleted = 'uncompleted',
}

export enum SortBy {
  title = 'title',
  date = 'date',
}

export enum SortOrder {
  asc = 'asc',
  desc = 'desc',
}
